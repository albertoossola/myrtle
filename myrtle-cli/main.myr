device {
    btn = in(pin=6);
    led = out(pin=26);
}

machine main {
    state entry {
        watchvar(var="btn") 
		>> debounce(period=50) 
		>> emit(items=[0, 0, 1, 1]) 
		>> setvar(var="led");
    }
}
